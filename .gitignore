assets/external/
.states
.web
# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Reflex
.web/
.states/
*.db
*.db-shm
*.db-wal

# Entorno
.env
.env.*
!.env.example

# Vendor (tuwayki-core se instala desde _vendor/ solo para Docker)
_vendor/

# Editores
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Logs temporales de dev
reflex_run*.log
reflex_run*.err
*.log
dev.err

# Reflex runtime lock
reflex.lock/*
# Excepción: versionamos package.json para fijar el override es-toolkit ^1.48.0
# (blindaje contra el bug recharts/compat CJS de es-toolkit <=1.47.0). bun.lock
# se mantiene ignorado: el build hace fresh-resolve pero con el piso ^1.48.0.
!reflex.lock/package.json

# Backups MySQL (generados por scripts/backup-mysql.sh)
backups/*.sql.gz

# Imagenes de prueba sueltas
test_burger.jpg
test_*.jpg

# Higgsfield CLI skills (herramienta local de generacion img/video, no del POS)
.agents/
.claude/skills/
skills-lock.json
